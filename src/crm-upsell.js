// Точки перехода из бесплатной визитки в CRM-систему.
//
// Правило продукта: визитка полностью бесплатна и самодостаточна.
// Реклама — ненавязчивая: мы не блокируем то, что человек уже делает,
// а показываем следующий шаг там, где он естественно возникает.
//
// Продукт намеренно называется «CRM-система» без бренда — по решению
// на этом этапе. Когда бренд раскрывается, достаточно поменять CRM_NAME.
export const CRM_NAME = 'CRM-система';
export const CRM_URL = 'https://eventory-mvp.vercel.app/';

// Каждая точка включается/выключается отдельно. Состав ещё не финализирован,
// поэтому здесь один переключатель, а не правки по всему коду.
export const UPSELL_POINTS = {
  // Заявка пришла — вести её дальше (воронка, история) нужно в CRM.
  leads: {
    enabled: true,
    eyebrow: 'Заявки',
    title: 'Заявки приходят к вам напрямую',
    text: 'Клиент пишет вам в Telegram или звонит. Чтобы вести заявки по этапам и не терять их — нужна CRM-система.',
    cta: 'Посмотреть CRM-систему'
  },
  // Из заявки — расчёт и КП. Главный сценарий CRM.
  quote: {
    enabled: true,
    eyebrow: 'Расчёт и КП',
    title: 'Посчитать смету и отправить КП',
    text: 'Расчёт стоимости, коммерческое предложение и счёт — в CRM-системе.',
    cta: 'Как это работает'
  },
  // Календарь занятости.
  calendar: {
    enabled: true,
    eyebrow: 'Календарь',
    title: 'Календарь занятости',
    text: 'Бронь дат, напоминания и предоплаты ведутся в CRM-системе.',
    cta: 'Открыть CRM-систему'
  },
  // Подпись на публичной визитке — её видит клиент владельца, а не он сам.
  // Самая деликатная точка: текст нейтральный, без давления.
  public: {
    enabled: true,
    eyebrow: '',
    title: '',
    text: '',
    cta: ''
  },
  // Аналитика просмотров: в визитке — общий счётчик, детали в CRM.
  analytics: {
    enabled: true,
    eyebrow: 'Аналитика',
    title: 'Кто смотрел визитку',
    text: 'Источники переходов и конверсия в заявку — в CRM-системе.',
    cta: 'Подробнее'
  }
};

export function upsellHref(pointId) {
  // utm — чтобы в CRM было видно, какая точка визитки реально приводит людей.
  const url = new URL(CRM_URL);
  url.searchParams.set('utm_source', 'card-app');
  url.searchParams.set('utm_medium', 'upsell');
  url.searchParams.set('utm_campaign', pointId);
  return url.toString();
}

export function activeUpsell(pointId) {
  const point = UPSELL_POINTS[pointId];
  return point?.enabled ? point : null;
}
