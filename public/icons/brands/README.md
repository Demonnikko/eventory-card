# Иконки соцсетей (визитка)

Официальные логотипы из [Simple Icons](https://simpleicons.org/) (CC0), собраны в цветные плитки 24×24.

Обновить файлы:

```bash
node scripts/sync-brand-icons.mjs
```

Имена файлов совпадают с `type` в редакторе визитки (`instagram`, `telegram`, `vk`, …).
